# Matriz de validação global

A matriz valida capacidades do VentureOps AI. Aponta para resultados e
evidências; não os reproduz. Actualizar apenas por evidência (nova validação ou
mudança de estado), pelo respectivo ID.

Estados: `Não iniciado` | `Em validação` | `Validado` | `Parcial` | `Falhado` |
`Bloqueado` | `Não aplicável`.

| ID | Capacidade | Fase | Critério | Estado | Evidência | Última validação |
|---|---|---|---|---|---|---|
| VAL-001 | Autenticação | MVP | Acesso autenticado e sessão segura | Validado | Escopo MVP completo (PR07–PR09): login/sessão/logout por cookie (HttpOnly, SameSite=Lax, Secure condicional), CSRF, conta controlada, recuperação por token único/expirável (invalida sessões), rate limiting persistente (PostgreSQL, sem Redis) auditado, perfil mínimo. 78 testes backend + 10 frontend; e2e e demos ao vivo (login/recuperação/rate limit/perfil). Hardening PR12: email case-insensitive (constraint `Lower(email)`), `createinitialuser` sem palavra-passe em CLI, rate limiting atómico (advisory lock) com retenção/limpeza. Ver `prompt_07/08/09/12_resultado.md`. | 2026-07-13 |
| VAL-002 | Isolamento entre empresas | MVP | Nenhum acesso transversal entre tenants | Parcial | Contexto de empresa derivado da Membership no servidor; acesso por id valida pertença (cruzado → 404 auditado; sem Membership → 403); bateria de isolamento com 2 empresas (8 testes) nos endpoints existentes. Módulo `Product` (F1-P03-PR02/PR04/PR06): listagem/detalhe/edição/ciclo de vida só da empresa do contexto; produto alheio → 404 indistinguível de inexistente (inclui archive/reactivate/mark-reviewed); filtros nunca atravessam empresas; tentativa cruzada auditada (`security.cross_org_attempt`, `entity_type=product`). 8 testes dedicados de isolamento cross-org + E2E ao vivo com 2 empresas (PR06). Ver `prompt_10/11_resultado.md` e `03_.../prompt_02/04/06_resultado.md`. **Parcial**: restantes módulos de domínio ainda não existem; suite completa em F1-P07/MVP-18. Hardening PR12: garantia de BD de uma Membership activa por utilizador + onboarding concorrente testado. Módulo documental (F1-P04-PR02): documento/produto alheios → 404 auditado; filtros e histórico nunca atravessam empresas. Módulo de decisões (F1-P04-PR04): responsável/produto/documento alheios → 400; decisão alheia → 404 auditado; substituição alheia → 404; listagem/filtros isolados. Módulo de pendências (F1-P04-PR05): responsável/produto/decisão alheios → 400; pendência alheia → 404 auditado; coerência product–decision; filtros/paginação isolados. Fecho F1-P04 (PR06): isolamento dos três módulos revalidado (testes dedicados de acesso cruzado com 2 empresas, 404 indistinguível + auditados). **Parcial (mantém-se):** suite transversal de isolamento em F1-P07/MVP-18. | 2026-07-14 |
| VAL-003 | Portefólio de produtos | MVP | CRUD e arquivo validados | Validado | Módulo Product completo (F1-P03, PR01–PR06): vários produtos geríveis; criação com ficha mínima (só `name`+`purpose`, defaults no servidor); consulta e edição; arquivo/reactivação (active↔archived, sem eliminação física); filtros (estado/responsável), paginação e ordenação; revisão explícita ("marcar como revisto" é a única fonte de `last_reviewed_at`); isolamento por empresa (produto alheio → 404 auditado; ciclo de vida alheio falha); concorrência optimista (409, sem lost update, estável em 3 rondas); UI React/TS funcional. Fecho (PR06): E2E ao vivo dos 21 passos + isolamento com 2 empresas; regressão 185 testes backend + 28 frontend + build; migração sem drift e reversível com dados preservados; health/Docker OK. 69 testes backend do módulo + 16 de componente. Ver `prompt_01–06_resultado.md`. **Fora do âmbito** (não validados aqui): agregados reais (documentos/decisões/pendências/execuções), nível de atenção e pesquisa. | 2026-07-13 |
| VAL-004 | Documentos e versões | MVP | Versões criadas e recuperáveis | Validado | API documental (F1-P04-PR02) + UI (PR03): criar/listar/consultar/editar/recuperar/marcar; conteúdo Markdown em ficheiros privados (chave/checksum no servidor), versões imutáveis e recuperáveis (recuperação cria nova versão sem apagar histórico); coordenação BD↔storage (objecto antes da BD; limpeza de órfão; `current_version` nunca aponta para objecto inexistente); concorrência optimista (409 sem lost update); marcadores `is_outdated`/`export_policy` e tipos filtráveis; preview sanitizada (endpoint do backend). **Fecho integrado (F1-P04-PR06):** validação ao vivo na ficha do produto (documento v1→v2→recuperação v3; `is_outdated`; `export_policy` allowed/confirm/**denied não omitido**; sem geração de pacote; preview com XSS/HTML/`javascript:`/`data:` neutralizados — 0 script/img, único `<a>` https com `rel="nofollow noopener"`); verificação na BD/armazenamento (checksums coincidentes, `current_version` sem órfãos, drift zero); 43 testes documentais (backend+componente) verdes na regressão. Ver `03_.../prompt_02/03/06_resultado.md`. | 2026-07-14 |
| VAL-005 | Decisões | MVP | Registo e associação a produto | Validado | Gestão de decisões (F1-P04-PR04): registo (título, contexto, decisão, responsável, data, impacto opcional), consulta e substituição; ciclo Activa→Substituída (nasce active, nunca eliminada nem reescrita); substituição atómica cria nova active, marca anterior superseded e liga a cadeia (`supersedes`/`replaced_by`, OneToOne linear); ligações opcionais a Product e documento `decisao_detalhada`, isoladas por empresa (responsável/produto/documento alheios → 400; decisão alheia → 404 auditado; tipo errado → 400); concorrência real em PostgreSQL (2 substituições simultâneas: uma vence, sem cadeia ramificada); sem DELETE/PATCH (405); auditoria evento 8 sem conteúdo. UI na ficha do produto com cadeia navegável e substituição confirmada. 24 testes backend + 4 de componente; demo ao vivo (criar doc+decisão, substituir, cadeia, eventos) confirmada na BD. **Fecho integrado (F1-P04-PR06):** cadeia Activa↔Substituída confirmada na ficha e na BD (nova active com `supersedes`, anterior superseded com `replaced_by`); sem edição destrutiva; concorrência estável em 3 rondas. Ver `03_.../prompt_04/06_resultado.md`. | 2026-07-14 |
| VAL-006 | Pendências administrativas | MVP | Estados e prazos validados | Validado | Gestão mínima de pendências (F1-P04-PR05): entidade `WorkItem` com os 5 tipos fechados (action/review/validation/obligation/decision_follow_up), prioridade (low/medium/high), produto obrigatório e decisão opcional; um único ciclo de vida `open→completed`/`open→cancelled` (finais imutáveis, sem reabertura nem eliminação); `is_overdue` calculado (prazo passado com `open`), nunca persistido; API criar/listar(filtros product/status/work_type/responsible/overdue, paginação)/detalhe/editar(só open)/concluir/cancelar, `expected_version`→409, sem DELETE; isolamento (responsável/produto/decisão alheios → 400; pendência alheia → 404 auditado; coerência product–decision); concorrência real (concluir vs cancelar concorrentes: uma vence); auditoria evento 9 sem notas. UI na ficha com sinal visual de vencida e cancelamento confirmado; sem conceitos de gestão de projectos. 30 testes backend + 6 de componente; demo ao vivo dos 5 tipos (conclusão, cancelamento, vencida, decision_follow_up ligada) confirmada na BD e auditoria. **Fecho integrado (F1-P04-PR06):** os 5 tipos e estados confirmados na ficha e na BD; estados finais não transitam; `is_overdue` calculado; concorrência (concluir vs cancelar) estável em 3 rondas. Ver `03_.../prompt_05/06_resultado.md`. | 2026-07-14 |
| VAL-007 | Funções organizacionais | MVP | Função utilizável numa execução | Validado | Catálogo de funções (F1-P05-PR01): `FunctionProfile` com `actor_type` fechado (human/ai/hybrid), `purpose`/`responsibilities` obrigatórios, `constraints` opcional, documento de instruções empresarial `instrucoes` opcional validado (mesma empresa, tipo exacto, sem produto, `current_version` válida), `requires_approval` com política SEC-HUM (ai/hybrid sempre True; human por defeito False; edição nunca enfraquece a regra), ciclo `active↔inactive` por operações explícitas (deactivate/reactivate; transição repetida → 409), edição com `expected_version` (409 sem lost update) que não altera estado; `status` inalterável por PATCH; sem DELETE; isolamento (função alheia → 404 auditado `entity_type=function`); auditoria evento 10 (`function.created`/`function.updated`) sem conteúdo integral; concorrência real em PostgreSQL (uma vence). API `/api/v1/functions` (list/create/detail/patch/deactivate/reactivate) + área **Funções** na UI (lista/filtros por estado e tipo/criação/edição/inactivação com confirmação/reactivação; inactiva identificada). 31 testes backend + 8 de componente; regressão 324 backend + 57 frontend + build; migração sem drift. Ver `05_.../prompt_01_resultado.md`. **Reforço (F1-P05-PR02):** a função é agora **usada** numa execução — `AIExecution` captura `function_snapshot` imutável (só campos aprovados) no instante da criação e só funções `active` são seleccionáveis (inactive → 400); alterações/inactivação posteriores da função não alteram o snapshot nem invalidam a execução. Ver `05_.../prompt_02_resultado.md`. **Reforço UI (F1-P05-PR03):** a ficha do produto ganha a secção **Execuções** onde o operador selecciona a função `active` (apenas activas são oferecidas; mostra actor_type/propósito/requires_approval) para preparar uma execução; o detalhe apresenta o `function_snapshot` congelado. Ver `05_.../prompt_03_resultado.md`. **Fecho integrado (F1-P05-PR06):** cenário E2E — função criada nos três tipos com `requires_approval` validado, gerida (inactivar → não seleccionável em novas execuções → 400; execução passada mantém `function_snapshot` e gera pacote; reactivar) e **efectivamente utilizada** numa execução; isolamento com 2 empresas; auditoria evento 10 com `correlation_id` sem conteúdo. Ver `05_.../prompt_06_resultado.md`. **Validado (escopo MVP).** | 2026-07-14 |
| VAL-008 | Pacote de contexto | MVP | Versões exactas preservadas | Validado | Fundação de execução (F1-P05-PR02): `AIExecution` + `ExecutionContextDocument` congelam o contexto no instante da criação — ligação a **versões documentais exactas** (nunca a versão actual do documento), ordenada e imutável (unique execution+order e execution+document_version; save/delete bloqueados); `instruction_version` aponta para a `current_version` exacta das instruções (ou null); snapshots imutáveis de função e produto gerados no servidor. Selecção valida empresa/produto (documento empresarial ou do produto da execução; outro produto/empresa → 400), `export_policy` (`denied` rejeitado, `confirm` aceite), não repetição da versão de instruções e não duplicação; `is_outdated` não bloqueia (aviso). Testes provam que alterações posteriores a documento/versão/função/produto não mudam a execução (versão actual não substitui a seleccionada). 41 testes backend; regressão 365 backend + 57 frontend; migração sem drift. Ver `05_.../prompt_02_resultado.md`. **Reforço UI (F1-P05-PR03):** selecção de versões documentais exactas no browser (empresariais ou do produto; `denied` desactivado, `confirm`/`is_outdated` com aviso mas seleccionáveis; versão histórica escolhível; ordenação Subir/Descer; instruções da função à parte, não duplicáveis); o detalhe apresenta os documentos por ordem com `version_number` e checksum, distinguindo o snapshot congelado dos marcadores actuais. 16 testes de componente; regressão 73 frontend + build. Ver `05_.../prompt_03_resultado.md`. **Geração no servidor (F1-P05-PR04):** serviço + API (`context-package/preview`|`download`) que montam um pacote **determinístico** de 7 secções a partir dos snapshots congelados e das DocumentVersion exactas (nunca a versão actual); `export_policy` aplicada e **reavaliada em cada geração** (denied bloqueia com 409 identificando bloqueadores sem conteúdo — incluindo denied superveniente e o documento de instruções; confirm exige confirmação explícita não memorizada; allowed incluído; is_outdated só aviso; contorno directo bloqueado); formatos `single_markdown` e ZIP (stdlib, nomes anti-traversal); checksum SHA-256 estável (sem timestamps, finais de linha normalizados); limite → 413; objecto ausente → 409 sem pacote parcial; só `prepared` gera, sem alterar estado; auditoria evento 12 sem conteúdo. 27 testes backend; regressão 392 backend; sem drift. Ver `05_.../prompt_04_resultado.md`. **UI e handoff manual (F1-P05-PR05):** `ContextPackagePanel` no detalhe da execução (só em `prepared`) permite rever, confirmar (checkbox por documento `confirm`, "Confirmar todos" com lista+confirmação adicional; `denied` bloqueado sem contorno; `is_outdated` aviso; instruções na mesma análise), pré-visualizar (texto não executável, checksum e manifesto), copiar (Clipboard API) e descarregar (`.md`/`.zip`, URL temporária revogada); confirmações só em memória e reavaliadas a cada geração; 409 recarrega a análise. Handoff manual simulado nos testes (gerar/confirmar/copiar/descarregar em manual_local/external; sem IA; execuções ficam `prepared`). 18 testes de componente; regressão 91 frontend + build. Ver `05_.../prompt_05_resultado.md`. **Fecho integrado (F1-P05-PR06):** cenário E2E — versões v1 preservadas após criar v2 (conteúdo/checksums das v1 no pacote; versão actual nunca substitui a seleccionada); pacote determinístico em ambos os formatos (checksums byte-idênticos entre regenerações — evidência registada); 7 secções em ordem, fontes/versões/checksums, hostil em DADOS; política `export_policy` no servidor (denied não seleccionável, confirm exige confirmação, denied superveniente → 409 sem conteúdo, allowed aplicado após repor); concorrência coerente (2 gerações simultâneas idênticas; alteração de política concorrente sem pacote parcial); objecto ausente → 409; ZIP sem traversal; auditoria evento 12 com `correlation_id` sem conteúdo; execuções permanecem `prepared`; nenhuma IA chamada. Regressão 396 backend + 91 frontend. Ver `05_.../prompt_06_resultado.md`. **Validado (escopo MVP).** Confirmação com modelo externo real em F1-P08/piloto. | 2026-07-14 |
| VAL-009 | Resultados de IA | MVP | Resultado associado à execução correcta | Não iniciado | — | — |
| VAL-010 | Revisão e aprovação | MVP | Nenhuma aplicação sem validação humana | Não iniciado | — | — |
| VAL-011 | Visão de atenção | MVP | Alertas explicáveis e coerentes | Não iniciado | — | — |
| VAL-012 | Auditoria | MVP | Operações críticas rastreáveis | Parcial | `AuditEvent` append-only + serviço (correlação, conteúdo proibido rejeitado). Emissores integrados: autenticação/falhas (PR07/09), empresa criação/alteração (PR10), acesso cruzado (PR11), produto criação/edição/ciclo de vida (F1-P03-PR02/PR04: `product.created`/`product.updated`/`product.archived` com apenas operação, transições e nomes de campos — reactivação e revisão usam `product.updated` com a operação nos metadados; sem `purpose`/`notes` integrais). Fecho PR06: E2E confirma criação/edição/arquivo/reactivação/revisão/tentativa cruzada auditadas, com `correlation_id` e 0 fugas de conteúdo. Ver `prompt_06/07/09/10/11_resultado.md` e `03_.../prompt_02/04/06_resultado.md`. Módulo documental (F1-P04-PR02): `document.created`/`document.updated`/`document.version_created`/`document.version_restored` com metadados mínimos (operação, versão, checksum abreviado, nomes de campos) e `storage.failure` em falha de armazenamento; 0 fugas de conteúdo Markdown (token único ausente dos eventos). Módulo de decisões (F1-P04-PR04): `decision.created`/`decision.updated` (operação, transição `active->superseded`, identificadores `supersedes`/`replaced_by`/`product_id`/`detail_document_id`), sem `context`/`decision_text` integrais. Módulo de pendências (F1-P04-PR05): `work_item.created`/`work_item.updated` (operação, transição `open->completed`/`open->cancelled`, `work_type`/`product_id`/`decision_id`), sem `notes` integrais. Fecho F1-P04 (PR06): auditados os eventos 5–9 + acesso cruzado + `storage.failure`; todos com `correlation_id`; inspecção das chaves de metadados dos três módulos confirma **ausência de conteúdo/context/decision/notes** (só operação/transição/ids/checksum abreviado). **Parcial**: consulta/linha temporal e consolidação global em F1-P07. | 2026-07-14 |
| VAL-013 | Exportação | MVP | Dados e Markdown exportáveis | Não iniciado | — | — |
| VAL-014 | Segurança de Markdown | MVP | Conteúdo renderizado sem XSS | Parcial | Renderização sanitizada no backend (F1-P04-PR02): estratégia *escape-first* (todo o texto escapado antes de qualquer transformação); só um subconjunto fixo de marcas seguras é emitido; HTML bruto, `<script>` e *event handlers* tornam-se texto inerte; código apresentado como texto; `href`/`src` só aceitam esquemas seguros (`javascript:`/`data:`/ofuscações com controlo removidos); endpoint `POST /documents/preview` aplica o mesmo limite e não guarda conteúdo. 7 testes (XSS, URLs perigosas, código, ofuscação, preview). Frontend (F1-P04-PR03): a pré-visualização usa exclusivamente o endpoint seguro do backend e insere apenas o HTML sanitizado devolvido; verificado ao vivo com conteúdo hostil (sem `<script>`/`<img>` vivos, único `<a>` https com `rel="nofollow noopener"`, `javascript:` removido, código como texto); 3 testes de componente (preview/script/URLs). Fecho integrado (F1-P04-PR06): preview revalidada ao vivo com bateria XSS/HTML/`javascript:`/`data:`/links/código (0 script/img, único `<a>` https com `rel`/`target`, sem `javascript:`/`data:text/html`). Ver `03_.../prompt_02/03/06_resultado.md`. **Pacote de contexto (F1-P05-PR04):** separação instruções/dados (secções 2/3 = instruções; secção 7 = DADOS), marcadores inequívocos de início/fim por documento e **declaração anti-injecção** antes dos dados; o Markdown original **não** é sanitizado nem transformado nem renderizado a HTML (embebido como texto); instrução hostil plantada num documento permanece em DADOS (testado). Ver `05_.../prompt_04_resultado.md`. **UI do pacote (F1-P05-PR05):** o conteúdo do pacote é apresentado como **texto não executável** (`<pre>`), **sem** `dangerouslySetInnerHTML` e sem renderizar HTML documental (HTML embebido aparece literal; nenhum `<script>` injectado — testado). Ver `05_.../prompt_05_resultado.md`. **Parcial (mantém-se):** anti-*prompt injection* (SEC-INJ-01) e restantes controlos de segurança consolidam-se em F1-P07. | 2026-07-14 |
| VAL-015 | Backup e recuperação | MVP | Recuperação validada | Não iniciado | — | — |
| VAL-016 | Deploy e rollback | MVP | Deploy e reversão executáveis | Parcial | Fundação apenas: health checks `/health/live` e `/health/ready` + CI mínimo build/testes (`.github/workflows/ci.yml`); ver `03_fase_1_mvp/pipelines/02_.../resultados_execucao/prompt_05_resultado.md`. **Deploy e rollback NÃO validados** (MVP-20/F1-P08). | 2026-07-12 |
