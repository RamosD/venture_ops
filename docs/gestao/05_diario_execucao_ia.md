# Diário de execução da IA

Índice cronológico compacto. Uma linha por execução ou reexecução. Não copiar
ficheiros alterados, comandos, logs, decisões ou riscos — esses detalhes
pertencem ao resultado do prompt e aos registos especializados.

| Data/hora | Fase/Pipeline | Prompt | Execução | Revisão | Resumo | Resultado |
|---|---|---|---|---|---|---|
| 2026-07-11 21:30 | F0 / F0-P00 | PR01 | Concluído | Pendente | Bootstrap da gestão da execução | `02_fase_0_preparacao/pipelines/00_bootstrap_gestao/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 21:42 | F0 / F0-P01 | PR01 | Concluído | Pendente | Backlog detalhado da Fase 0 criado | `02_fase_0_preparacao/pipelines/01_planeamento_fase_0/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 22:05 | F0 / F0-P01 | PR02 | Concluído | Pendente | Quatro pipelines da Fase 0 criadas (13 prompts) | `02_fase_0_preparacao/pipelines/01_planeamento_fase_0/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-11 22:31 | F0 / F0-P02 | PR01 | Concluído | Pendente | Segmento inicial e caso de uso principal propostos | `02_fase_0_preparacao/pipelines/02_escopo_e_caso_uso/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 22:41 | F0 / F0-P02 | PR02 | Concluído | Pendente | Fluxo funcional e limites do MVP propostos | `02_fase_0_preparacao/pipelines/02_escopo_e_caso_uso/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-11 22:49 | F0 / F0-P03 | PR01 | Concluído | Pendente | Modelo funcional do MVP proposto (8 entidades núcleo) | `02_fase_0_preparacao/pipelines/03_modelo_funcional/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 22:53 | F0 / F0-P03 | PR02 | Concluído | Pendente | Estados e transições das entidades propostos | `02_fase_0_preparacao/pipelines/03_modelo_funcional/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-11 22:56 | F0 / F0-P03 | PR03 | Parcial | Pendente | Ficha administrativa proposta; teste real pendente | `02_fase_0_preparacao/pipelines/03_modelo_funcional/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-11 22:59 | F0 / F0-P04 | PR01 | Concluído | Pendente | Fronteira BD/Markdown e versionamento propostos | `02_fase_0_preparacao/pipelines/04_dados_contexto_seguranca/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 23:03 | F0 / F0-P04 | PR02 | Concluído | Pendente | Pacote de contexto proposto e testado (injecção resistida) | `02_fase_0_preparacao/pipelines/04_dados_contexto_seguranca/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-11 23:05 | F0 / F0-P04 | PR03 | Bloqueado | N/A | Segurança bloqueada: faltam artefactos 08/09 (RB-20260711-01) | `02_fase_0_preparacao/pipelines/04_dados_contexto_seguranca/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-11 23:10 | F0 / F0-P05 | PR01 | Concluído | Pendente | Levantamento de stack: repositório greenfield (só docs) | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-11 23:13 | F0 / F0-P05 | PR02 | Concluído | Pendente | Modelo de utilizadores/empresas proposto; RB-01 em mitigação | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-11 23:20 | F0 / F0-P05 | PR03 | Concluído | Pendente | Regras da visão de atenção propostas (5 sinais) | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-11 23:25 | F0 / F0-P05 | PR04 | Concluído | Pendente | Plano do piloto proposto (participantes a identificar) | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-11 23:28 | F0 / F0-P05 | PR05 | Bloqueado | N/A | Fecho bloqueado: falta artefacto 10 e aprovações (RB-20260711-01) | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-12 | Governação | — | Concluído | N/A | Aprovação humana deixa de ser gate (DEC-20260712-01/02); F0-P04-PR03 pronto para reexecução | `docs/gestao/02_log_decisoes_execucao.md` |
| 2026-07-12 14:21 | F0 / F0-P04 | PR03 | Concluído (reexec.) | Não revista | Reexecução concluída: artefacto 10 (segurança) produzido; RB-20260711-01 resolvido | `02_fase_0_preparacao/pipelines/04_dados_contexto_seguranca/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-12 14:43 | F0 / F0-P05 | PR05 | Concluído (reexec.) | Não revista | Revisão cruzada e artefacto 12; decisão de saída proposta (Opção B, com reservas); Fase 0 pronta p/ transição, não concluída | `02_fase_0_preparacao/pipelines/05_stack_piloto_e_fecho/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-12 15:20 | F0 / Fecho | — | Concluído | Revista | Teste real da ficha (VentureOps AI) e participante mínimo (Aldino Ramos) confirmados; INC-01 a INC-04 resolvidas; saída da Fase 0 confirmada (DEC-20260712-04); Fase 0 concluída com reservas; decomposição da Fase 1 autorizada (não iniciada) | `02_fase_0_preparacao/artefactos/12_decisao_saida_fase_0.md` |
| 2026-07-12 16:04 | F1 / F1-P01 | PR01 | Concluído | Não revista | Início formal da Fase 1: backlog detalhado criado; MVP-01..23 decompostos (39 cap., 40 hist., 81 req., 110 tarefas); 7 pipelines recomendadas; nenhuma implementação iniciada | `03_fase_1_mvp/pipelines/01_planeamento_mvp/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-12 16:25 | F1 / F1-P01 | PR02 | Concluído | Não revista | Mapa global F1-P02..P08 criado; F1-P02 detalhada (11 prompts); pipelines futuras em planeamento just-in-time; 4 clarificações (DEC-20260712-05); nenhum código | `03_fase_1_mvp/pipelines/01_planeamento_mvp/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-12 17:10 | F1 / F1-P01 | PR03 | Concluído | Não revista | Correcção de dependências técnicas de F1-P02: estados de fecho da Fase 0 actualizados; CustomUser desde a 1.ª migração; fundação User/Org/Membership em PR02; `/api/system/ping` (PR01→PR03); PR06/PR09/PR10/PR11 ajustados; DEC-20260712-06; nenhum código | `03_fase_1_mvp/pipelines/01_planeamento_mvp/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-12 17:55 | F1 / F1-P02 | PR01 | Concluído | Não revista | Esqueleto Django+DRF (11 módulos vazios), config por ambiente validada, `/api/system/ping` (200, sem BD, teste verde), 14 decisões de arranque; frontend reservado; sem migrações/auth/BD | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-12 18:55 | F1 / F1-P02 | PR02 | Concluído | Não revista | PostgreSQL por env; CustomUser (UUID, email único, AUTH_USER_MODEL) + Organisation + Membership; 1.ª migração aplica/reverte em base vazia; convenção de entidade empresarial; 16 testes verdes; sem auth/onboarding | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-12 19:45 | F1 / F1-P02 | PR03 | Concluído | Não revista | Frontend React/TS (Vite) + cliente HTTP central; página consome `/api/system/ping` (a carregar/disponível/erro); build + 2 testes verdes; sem auth/store/UI de domínio | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-12 21:55 | F1 / F1-P02 | PR04 | Concluído | Não revista | Adaptador de armazenamento filesystem privado (chaves no servidor, anti-traversal, checksum SHA-256, extensão S3 não implementada) + Docker Compose (db+backend+frontend+volume privado); compose up integrado; 25 testes backend; sem S3 | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-12 22:45 | F1 / F1-P02 | PR05 | Concluído | Não revista | Health checks `/health/live` (processo) e `/health/ready` (BD+armazenamento, 503 controlado); testes base consolidados (34) + testes de config; CI mínimo build/testes sem deploy; VAL-016 Parcial (sem deploy/rollback) | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-12 23:35 | F1 / F1-P02 | PR06 | Concluído | Não revista | Auditoria append-only: `AuditEvent` (actor/empresa opcionais SET_NULL + snapshots, correlação) + serviço `record_event` (lista fechada, conteúdo proibido rejeitado); update/delete bloqueados; migração aplica/reverte; 48 testes; VAL-012 Parcial | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_06_resultado.md` |
| 2026-07-13 00:35 | F1 / F1-P02 | PR07 | Concluído | Não revista | Autenticação backend: conta inicial por comando controlado; login/sessão/logout por cookie (HttpOnly/SameSite/Secure-condicional); CSRF exigido; erros genéricos (sem enumeração); eventos auth auditados; 62 testes; VAL-001 Parcial | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_07_resultado.md` |
| 2026-07-13 01:40 | F1 / F1-P02 | PR08 | Concluído | Não revista | Ecrãs de autenticação (login/área autenticada/logout); cliente HTTP central com CSRF+credenciais+sessão expirada; sem credenciais persistidas; proxy corrigido (changeOrigin:false); e2e testes (8) + browser real; VAL-001 Parcial | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_08_resultado.md` |
| 2026-07-13 03:05 | F1 / F1-P02 | PR09 | Concluído | Não revista | Recuperação por token único/expirável (invalida sessões; email dev consola); rate limiting persistente PostgreSQL (sem Redis, auditado); perfil mínimo; 78 testes backend + 10 frontend; demos live (429/recuperação/perfil); VAL-001 Validado | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_09_resultado.md` |
| 2026-07-13 04:15 | F1 / F1-P02 | PR10 | Concluído | Não revista | Onboarding: Organisation activa + Membership Owner (transaccional, select_for_update; 2.ª empresa bloqueada); edição mínima autorizada no servidor; auditoria criação/alteração; sem novas migrações; 88 backend + 12 frontend; onboarding no browser; VAL-002 Parcial | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_10_resultado.md` |
| 2026-07-13 09:10 | F1 / F1-P02 | PR11 | Concluído | Não revista | Contexto de empresa derivado da Membership (sem Membership → 403; id alheio → 404 auditado); bateria de isolamento com 2 empresas (8 testes); 96 backend + 12 frontend; VAL-002/VAL-012 parciais; **F1-P02 concluída 11/11** | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_11_resultado.md` |
| 2026-07-13 11:20 | F1 / F1-P02 | PR12 | Concluído | Revista | Hardening: email case-insensitive (constraint Lower(email) + migração com verificação de colisões); createinitialuser sem palavra-passe em CLI (getpass/--noinput + validadores); retenção + purge de rate limiting; rate limiting atómico (advisory lock) e constraint de Membership activa única; concorrência estável; 116 backend + 12 frontend; F1-P02 12/12 | `03_fase_1_mvp/pipelines/02_fundacao_autenticacao_isolamento/resultados_execucao/prompt_12_resultado.md` |
| 2026-07-13 12:35 | F1 / F1-P03 | PR01 | Concluído | Não revista | Modelo `Product` + 1.ª migração do módulo portfolio + testes estruturais; isolamento por Organisation (OrganisationScopedModel), ficha mínima (5 obrigatórios + opcionais), estados Activo/Arquivado, `last_reviewed_at` inicializada na criação e desligada de edições comuns, `version` para concorrência optimista, `attention_level` não persistido; migração aplica/reverte em base vazia e existente; 135 backend verdes; sem API/UI; **F1-P03 em execução 1/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-13 13:40 | F1 / F1-P03 | PR02 | Concluído | Não revista | API CRUD do produto (`GET`/`POST` `/products`, `GET`/`PATCH` `/products/{id}`; sem DELETE): criar só com `name`+`purpose`, empresa derivada no servidor, responsável=utilizador por defeito (alheio rejeitado), concorrência optimista com `expected_version` (409 sem lost update), produto alheio → 404 indistinguível + tentativa cruzada auditada, `product.created`/`product.updated` só com operação+nomes de campos; 18 testes API (37 no módulo); 153 backend verdes; smoke ao vivo; VAL-003 Parcial; **F1-P03 2/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-13 14:45 | F1 / F1-P03 | PR03 | Concluído | Não revista | Interface inicial do portefólio (React/TS) sobre a API de PR02: `PortfolioWorkspace`/`ProductList`/`ProductCreateForm`/`ProductDetail`/`ProductEditForm` montados após o onboarding; criação a dois campos (Nome+Propósito) com defaults do backend na ficha; edição envia `version` e trata 409 sem sobrescrita (aviso+recarregar); cliente HTTP central reutilizado (sem store/router novos); 8 testes de componente (20 frontend) + build; validação ao vivo no browser (criação/ficha/edição, `version` 1→2, `last_reviewed_at` intacta ao nível do timestamp); VAL-003 Parcial; **F1-P03 3/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-13 15:50 | F1 / F1-P03 | PR04 | Concluído | Não revista | Backend completo do portefólio: arquivo/reactivação (transições active↔archived, sem eliminação), "marcar como revisto" (única fonte de `last_reviewed_at`, CLR-02), filtros (estado/responsável), paginação (page/page_size, máx 100) e ordenação (updated_at desc, id); todas as operações atómicas com `expected_version` e 409 em versão obsoleta; auditoria por operação (archive→product.archived; reactivate/mark_reviewed→product.updated) sem conteúdo integral; teste real de concorrência em PostgreSQL (uma vence, outra 409, sem lost update); 24 testes novos (61 no módulo); 177 backend verdes; sem drift; smoke ao vivo; VAL-002/003/012 reforçadas (Parciais); **F1-P03 4/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-13 17:05 | F1 / F1-P03 | PR05 | Concluído | Não revista | UI completa do portefólio/ficha (React/TS) sobre PR04: filtros por estado (Activos/Arquivados/Todos) e responsável ("Apenas os meus"), paginação (Anterior/Seguinte); ficha stateful com editar/arquivar (confirmação)/reactivar/marcar como revisto (confirmação explícita) e recarregar após 409; `last_reviewed_at` só muda após revisão explícita; produto arquivado com banner e edição bloqueada; área de agregados só informativa (sem dados/contagens simuladas); cliente HTTP central reutilizado (sem store/router/UI-lib novos); 16 testes de componente (28 frontend) + build; demonstração ao vivo do ciclo completo com estados confirmados no backend; VAL-003 (UI) reforçada Parcial; **F1-P03 5/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-13 18:35 | F1 / F1-P03 | PR06 | Concluído | Não revista | Validação integrada e fecho da F1-P03: E2E ao vivo dos 21 passos (defaults, edição sem tocar `last_reviewed_at`, revisão explícita, 409 sem lost update, arquivo/reactivação, filtros/paginação, auditoria) + isolamento com 2 empresas (GET/PATCH/archive/reactivate/mark-reviewed alheios → 404 auditado; lista não mistura); concorrência estável 3×; migração sem drift, reverte/reaplica com dados preservados; regressão 185 backend + 28 frontend + build; health live/ready 200; Docker Compose healthy; +8 testes de isolamento (sem correcção de código); VAL-003 **Validada**, VAL-002/VAL-012 Parciais; **F1-P03 Concluída 6/6** | `03_fase_1_mvp/pipelines/03_portefolio_ficha_produto/resultados_execucao/prompt_06_resultado.md` |
| 2026-07-13 19:55 | F1 / F1-P04 | PR01 | Concluído | Não revista | Fundação documental persistente: `Document` (título, tipo fechado, produto opcional da mesma empresa, `current_version`, marcadores `is_outdated`=false/`export_policy`=confirm só na BD, `version` de concorrência) + `DocumentVersion` imutável (número sequencial único por documento, `storage_key`/checksum/byte_size gerados no servidor, autor, resumo; sem campo de conteúdo — corpo só no armazenamento privado); enumerações fechadas de 5 tipos e 3 políticas com constraints de BD; migração `documents/0001` aplica em base vazia e dev, reverte/reaplica, sem drift; 22 testes novos, 207 backend verdes; sem API/UI/escrita real no armazenamento; **F1-P04 em execução 1/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-13 21:40 | F1 / F1-P04 | PR02 | Concluído | Não revista | API documental completa (`/api/v1/documents`): criar (v1+objecto privado), listar (metadados, filtros tipo/produto/is_outdated/export_policy, paginação), consultar (metadados+conteúdo da versão actual), editar (nova versão imutável e/ou metadados, `expected_version`→409 sem lost update), histórico, consulta de versão exacta, recuperar (nova versão a partir de anterior, sem apagar histórico) e preview sanitizada; conteúdo Markdown só em ficheiros privados (chave/checksum no servidor); coordenação BD↔storage (objecto antes da BD, limpeza de órfão em falha, `storage.failure`; limitação residual documentada sem fila/2PC); renderizador seguro autocontido escape-first (HTML/scripts/handlers→texto; `javascript:`/`data:`/ofuscações removidos; código como texto); marcadores e tipos filtráveis; isolamento 404 auditado; auditoria eventos 5–7 sem conteúdo integral; concorrência real testada em PostgreSQL; 32 testes novos, 239 backend verdes, sem drift; sem UI/exportação/decisões/pendências; VAL-004/VAL-014 Parciais; **F1-P04 em execução 2/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-13 23:35 | F1 / F1-P04 | PR03 | Concluído | Não revista | Experiência documental no frontend (React/TS) sobre a API de PR02, integrada na ficha do produto: `DocumentSection` + `DocumentList`/`DocumentCreateForm`/`DocumentDetail`/`DocumentEditor`/`DocumentPreview`/`DocumentHistory`; listagem só de metadados (filtro por tipo, paginação, estados vazio/carregamento/erro); criação com os 5 tipos fechados (labels PT, sem tipos arbitrários) e marcadores; editor simples com `expected_version`, resumo de alteração e 409→recarregar (sem sobrescrita); pré-visualização usa EXCLUSIVAMENTE o endpoint seguro do backend (único ponto com HTML, e só o HTML sanitizado devolvido) — não executa script/handler/`javascript:`/`data:`; histórico com abrir versão exacta e recuperação com confirmação explícita (cria nova versão, nunca elimina); marcadores `is_outdated`/`export_policy` com aviso de `denied` (sem omitir o documento); substitui só a parte de documentos do placeholder (decisões/pendências/execuções continuam indisponíveis; sem contagens simuladas); 11 testes de componente (39 frontend) + build tsc/vite; demo ao vivo (Docker reconstruído) com XSS neutralizado, v1→v2→recuperação v3, marcadores, e confirmação na BD/armazenamento (checksums coincidentes) e auditoria limpa; VAL-004/VAL-014 reforçadas (Parciais); **F1-P04 em execução 3/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-14 01:20 | F1 / F1-P04 | PR04 | Concluído | Não revista | Gestão de decisões (MVP-08): modelo `Decision` (UUID, organisation, title, context, decision_text, responsible, decided_at, impact, status active/superseded, product/detail_document opcionais, `supersedes` OneToOne com inverso `replaced_by`, `version`) + migração aplicada/reversível sem drift; API `/api/v1/decisions` criar/listar(filtros product/status, paginação)/detalhe/substituir, sem DELETE nem PATCH (405 — a decisão histórica não é reescrita); substituição atómica (`select_for_update`, valida estado+versão) cria nova active, marca anterior superseded e liga a cadeia, preservando o histórico; já superseded → 409, versão obsoleta → 409, concorrência real testada (2 substituições simultâneas: uma vence, cadeia linear garantida por unicidade OneToOne); isolamento (responsável/produto/documento alheios → 400; decisão alheia → 404 auditado; documento tem de ser da mesma empresa e tipo `decisao_detalhada`, coerência com o produto); auditoria evento 8 (`decision.created`/`decision.updated`) só com operação/transição/identificadores, sem conteúdo; UI `DecisionSection` na ficha do produto (lista por estado, criação, detalhe com cadeia navegável, substituir com aviso, ligação ao documento de detalhe); 24 testes backend + 4 de componente; 263 backend + 43 frontend + build verdes; demo ao vivo (doc `decisao_detalhada` → decisão ligada → substituição → cadeia) confirmada na BD (estados/eventos); dados de demo mantidos (prática vigente); VAL-005 Validado; VAL-002/VAL-012 reforçadas (Parciais); **F1-P04 em execução 4/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-14 03:20 | F1 / F1-P04 | PR05 | Concluído | Não revista | Gestão mínima de pendências (MVP-09): modelo `WorkItem` (UUID, organisation, product obrigatório, decision opcional, title, work_type dos 5 tipos fechados, responsible, priority low/medium/high, due_at, notes, status open/completed/cancelled, completed_at/cancelled_at, version) + migração aplicada/reversível sem drift; `is_overdue` calculado (prazo passado com open), nunca persistido; um único ciclo de vida (open→completed/open→cancelled, finais imutáveis, sem reabertura nem eliminação); API `/api/v1/work-items` criar/listar(filtros product/status/work_type/responsible/overdue, paginação determinística)/detalhe/editar(só open)/concluir/cancelar, expected_version→409, 404 cruzado, sem DELETE (405); isolamento (responsável/produto/decisão alheios → 400; coerência product–decision; pendência alheia → 404 auditado); concorrência real (concluir vs cancelar concorrentes: uma vence, sem dupla transição); auditoria evento 9 (`work_item.created`/`work_item.updated`) só com operação/transição/tipo/identificadores, sem notas; UI `WorkItemSection` na ficha (lista curta com sinal de vencida calculado, criar/editar/concluir/cancelar com confirmação, filtros mínimos, ligação opcional a decisão), sem quadro kanban/sprint/backlog/bug/história/dependência; endurecimento dos testes de migração (portfolio/documents/decisions/work_items restauram o esquema no setUp/tearDown → independência de ordem entre TransactionTestCase); 30 testes backend + 6 de componente; 293 backend + 49 frontend + build verdes; demo ao vivo dos 5 tipos (conclusão, cancelamento, vencida, decision_follow_up ligada) confirmada na BD/auditoria; VAL-006 Validado; VAL-002/VAL-012 reforçadas (Parciais); **F1-P04 em execução 5/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-14 10:35 | F1 / F1-P04 | PR06 | Concluído | Não revista | Validação integrada e fecho da F1-P04: E2E ao vivo dos três módulos na ficha do produto (documentos v1→v2→recuperação v3 + preview XSS/HTML/`javascript:`/`data:` neutralizada — 0 script/img, único `<a>` https com rel/target; cadeia de decisão Activa↔Substituída; 5 pendências com conclusão/cancelamento/vencida e decision_follow_up ligada); integração deriva agregados das associações (sem campos duplicados no Product nem contagens simuladas; `denied` não omitido; sem geração de pacote; Execuções indisponível); isolamento dos três módulos (acesso cruzado → 404 indistinguível auditado) por testes dedicados com 2 empresas; concorrência real (edições/recuperações/substituições/transições) estável em 3 rondas — uma vence, sem lost update/versões duplicadas/cadeias divergentes; armazenamento verificado (4 objectos, checksums coincidentes, `current_version` sem órfãos, chaves no servidor, não-público, path traversal rejeitado; limitação residual honesta sem 2PC/fila); auditoria eventos 5–9 + cross-org + storage.failure, todos com correlation_id, chaves de metadados sem conteúdo/context/decision/notes; migrações drift zero, aplicam em base vazia/existente, reversibilidade estrutural em base controlada (revert remove tabelas do módulo, não preserva conteúdo; outras apps intactas); regressão 293 backend + 49 frontend + build + health live/ready + Docker healthy; nenhum defeito encontrado (nenhuma correcção de código); VAL-004/VAL-005/VAL-006 Validadas, VAL-002/VAL-012/VAL-014 mantêm-se Parciais; sem antecipar F1-P05; **F1-P04 Concluída 6/6** | `03_fase_1_mvp/pipelines/04_documentos_decisoes_pendencias/resultados_execucao/prompt_06_resultado.md` |
| 2026-07-13 23:40 | F1 / F1-P05 | PR01 | Concluído | Não revista | Catálogo de funções organizacionais (MVP-10): modelo `FunctionProfile` (UUID, organisation, name, actor_type fechado human/ai/hybrid, purpose/responsibilities obrigatórios, constraints opcional, instruction_document opcional, requires_approval, status active/inactive, version) + migração `functions/0001` aplica em base vazia/reverte em base controlada, sem drift; regras: função ≠ papel de autorização, nasce active, sem eliminação física, documento de instruções validado (mesma empresa, tipo exacto `instrucoes`, empresarial sem produto, `current_version` válida), status inalterável por PATCH; política `requires_approval` SEC-HUM (ai/hybrid sempre True — rejeita False explícito; human por defeito False; edição para ai/hybrid força True) sem mecanismo genérico de políticas; API `/api/v1/functions` list/create + detail/patch (expected_version→409, não altera estado) + deactivate/reactivate (active↔inactive, transição repetida→409), sem DELETE (405); listagem só da empresa, filtros status (active por defeito, inactive, all) e actor_type, paginação determinística; isolamento (função alheia → 404 auditado `security.cross_org_attempt` entity_type=function); concorrência real em PostgreSQL (editar vs inactivar concorrentes: uma vence, version +1); auditoria evento 10 (`function.created`/`function.updated`) só com operation/actor_type/transition/fields, sem purpose/responsibilities/constraints/instruções (provado com tokens únicos); UI área empresarial **Funções** (FunctionsWorkspace em OrganisationGate, sem router novo): lista/filtros/criação/edição/inactivação com confirmação/reactivação, selecção opcional de documento de instruções (só empresariais com versão), inactiva identificada, regra SEC-HUM reflectida (checkbox forçada para ai/hybrid); nenhum módulo anterior alterado (só routing acrescentado); não implementado execução/snapshot/pacote/resultados/aprovação/templates/permissões; 31 testes backend + 8 de componente; 324 backend + 57 frontend + build verdes; VAL-007 Parcial (falta uso numa execução); VAL-002/VAL-012 reforçadas (Parciais); **F1-P05 em execução 1/6** | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-14 00:55 | F1 / F1-P05 | PR02 | Concluído | Não revista | Fundação de execução assistida (MVP-11): modelos `AIExecution` (UUID, organisation, product/function_profile/requested_by obrigatórios PROTECT, title/objective/request_instructions/expected_output_format obrigatórios, constraints opcional, execution_mode manual_local/manual_external, status enum oficial completa editable=False, function_snapshot/product_snapshot JSON editable=False, instruction_version FK→DocumentVersion editable=False, version) e `ExecutionContextDocument` (id UUID, execution/document_version PROTECT editable=False, order, purpose curto, created_at; imutável — save/delete/queryset bloqueados; unique execution+order e execution+document_version) + migração `executions/0001` aplica/reverte em base controlada, sem drift; política central de estados `transitions.py` (prepared→result_pending_validation; result_pending_validation→approved/rejected/prepared correcção; approved→completed; restantes inválidas; rejected/completed terminais) para consumo por F1-P06, sem executar comandos funcionais; criação sempre em prepared (sem endpoint genérico de estados); snapshots gerados só no servidor (função: id/name/actor_type/purpose/responsibilities/constraints/requires_approval; produto: id/name/purpose/status + phase/target_audience quando existem, sem credenciais/email/dados pessoais), imutáveis a alterações/inactivação posteriores; instruction_version = current_version exacta das instruções (ou null; denied→execução rejeitada, confirm→aceite sujeita a confirmação na geração); contexto por versões documentais exactas (nunca a versão actual), empresarial ou do produto da execução, denied rejeitado/confirm aceite/is_outdated só aviso, versão de instruções não repetível como dados, ≥1 versão, ordem pela posição na lista; API `/api/v1/executions` list (filtros product/status/function_profile/execution_mode, paginação determinística, sem conteúdo integral) + create (rejeita organisation/status/snapshots/internos, transacção, audita) + detail (metadados+snapshots+instruction_version+documentos ordenados com título/tipo/version_number/checksum/export_policy actual/is_outdated, sem conteúdo), sem PATCH/DELETE (405); isolamento (execução alheia → 404 auditado `security.cross_org_attempt` entity_type=execution; entradas alheias → 400); auditoria evento 11 (`execution.created`) só com operation/product_id/function_profile_id/execution_mode/document_count/document_version_ids/instruction_version_id, sem objective/instruções/constraints/snapshots/conteúdo (provado com tokens únicos); nenhum módulo anterior alterado (só routing); não implementado UI/pacote/IA/resultados/revisão/aplicação/transições funcionais; 41 testes backend (28 requisitos + matriz de transições + imutabilidade + migração); 365 backend + 57 frontend verdes; VAL-007 reforçada (Parcial), VAL-008 Parcial, VAL-009 não validável sem F1-P06; VAL-002/VAL-012 reforçadas (Parciais); **F1-P05 em execução 2/6** | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-14 03:45 | F1 / F1-P05 | PR03 | Concluído | Não revista | Interface de preparação da execução (MVP-11.T3): secção **Execuções** real na ficha do produto (substitui o aviso "indisponíveis"; mantém DocumentSection/DecisionSection/WorkItemSection); componentes `ExecutionSection`/`ExecutionList`/`ExecutionCreateForm`/`ContextDocumentSelector`/`FunctionSnapshotView`/`ExecutionDetail` + `api/executions.ts` + labels; formulário com title/objective/request_instructions/constraints opcional/expected_output_format/execution_mode (manual_local/manual_external), selecção de função só `active` (mostra actor_type/propósito/requires_approval; não cria/edita função no formulário), selecção de ≥1 versão documental exacta com ordenação explícita e purpose opcional; documentos candidatos = empresariais + do produto actual (nunca de outros produtos), com título/tipo/version_number/checksum abreviado/is_outdated/export_policy; `denied` desactivado e não seleccionável, `confirm` com aviso de confirmação na geração, `is_outdated` com aviso mas seleccionável; escolha de versão exacta/histórica via histórico do documento; documento de instruções da função apresentado à parte e não duplicável; ordenação Subir/Descer (sem drag-and-drop nem biblioteca); após criação abre o detalhe congelado (status Preparada, function_snapshot e product_snapshot com aviso de retrato congelado, instruction_version, documentos por ordem com versão e checksum, distinção snapshot vs marcadores actuais); sem edição/eliminação/transições/IA/resultados; cliente HTTP central reutilizado; 400/403/404/409 tratados; submissão duplicada evitada (busy+disabled, uma só criação); duas adições de leitura mínimas e aditivas ao módulo documental autorizadas pelo prompt (filtro `empresarial=true|false` no GET /documents; `id` da versão em DocumentVersionSerializer) — suite documental 54 OK; teste de portefólio actualizado para a secção real; 16 testes de componente (22 casos do guião); regressão 365 backend + 73 frontend + build verdes; demonstração ponta a ponta coberta pelos testes de componente + build (demo no browser exigiria reconstruir imagens Docker — não efectuada; imutabilidade perante alterações posteriores garantida/testada no backend PR02); VAL-007/VAL-008 reforçadas (Parciais); VAL-002/VAL-012 reforçadas (Parciais); **F1-P05 em execução 3/6** | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-14 05:00 | F1 / F1-P05 | PR04 | Concluído | Não revista | Geração do pacote de contexto (MVP-12): serviço `context_package.py` + endpoints `POST /executions/{id}/context-package/preview` e `/download`; pacote determinístico de 7 secções na ordem fixa (Objectivo/Função/Instruções do pedido/Produto/Restrições/Formato esperado/Documentos-DADOS) construído a partir dos snapshots congelados e das DocumentVersion exactas (nunca current_version); função usa function_snapshot + conteúdo exacto de instruction_version (id/version_number/checksum, entre marcadores); produto usa product_snapshot (sem dados pessoais); secção 5 combina constraints função+execução com declaração anti-injecção; secção 7 lê só as versões referenciadas, com fonte/Document id/DocumentVersion id/título/tipo/version_number/checksum/is_outdated/export_policy actual, marcadores início/fim inequívocos e declaração anti-injecção antes dos dados; Markdown original não sanitizado nem transformado nem renderizado a HTML (embebido como texto; instrução hostil permanece em DADOS); export_policy aplicada no servidor e reavaliada em cada geração com linhas Document bloqueadas (select_for_update) — denied bloqueia com 409 identificando denied_document_ids sem conteúdo (inclui denied superveniente e o documento de instruções), confirm exige confirmação explícita (confirmed_document_ids validada; não memorizada; 409 se em falta), allowed incluído, is_outdated só aviso, contorno directo bloqueado; formatos single_markdown (um .md) e separate_files (ZIP zipfile ZIP_STORED, manifest.json + secções numeradas + documentos/NN por ordem, nomes gerados no servidor anti path traversal), sem dependência externa; determinismo — sem timestamps no conteúdo/ZIP (data fixa), finais de linha normalizados a \n, ordem estável, checksum SHA-256 do pacote devolvido, manifesto com versões/checksums das fontes, mesmos bytes em ambas as gerações; limite CONTEXT_PACKAGE_MAX_BYTES (nova definição) → 413 antes de devolver; objecto de versão ausente → 409 sem pacote parcial + storage.failure; só execução prepared gera (outros → 409); geração não altera estado nem cria resultado; preview devolve warnings/manifest/checksum/content (single) ou manifest+files (separate, sem ZIP), sem conteúdo quando bloqueia; download devolve .md/.zip com Content-Disposition seguro e X-Package-Checksum, nunca envia a IA; auditoria evento 12 (`context_package.exported`) distingue preview/copy/download, regista execution id/modo/formato/checksum/ids de versões/documentos confirmados/destino genérico, sem objective/instruções/snapshots/conteúdo/nomes de ficheiro (provado com tokens); bloqueios auditados como denied; acesso cruzado → 404 + security.cross_org_attempt; nenhum módulo anterior alterado; não implementado UI/importação de resultado/IA; 27 testes backend (34 requisitos do guião); 392 backend + 73 frontend verdes; sem drift; VAL-008 reforçada (Parcial até UI), VAL-014 reforçada (Parcial até F1-P07), VAL-012 Parcial; **F1-P05 em execução 4/6** | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-14 06:05 | F1 / F1-P05 | PR05 | Concluído | Não revista | Interface do pacote e handoff manual (MVP-12.C1/C2): `ContextPackagePanel` no detalhe da execução (só em `prepared`; nos restantes estados explicação sem gerar; snapshots e lista de versões mantidos); análise de política por documento não contornável — `allowed` indicação simples, `confirm` checkbox individual inicialmente desmarcada, "Confirmar todos" só com lista+confirmação adicional deliberada, `denied` bloqueado sem botão de contorno, `is_outdated` aviso visível, documento de instruções da função na mesma análise; confirmações só em memória (nunca localStorage) e reavaliadas a cada geração; a UI não confia só no estado local — geração chama sempre o backend e um 409 apresenta o motivo e recarrega a análise (cobre denied superveniente); avisos por modo (manual_external partilha externa / manual_local ambiente autorizado) e aviso de que o conteúdo poderá sair da aplicação; preview mostra as 7 secções como texto não executável (sem dangerouslySetInnerHTML, sem renderizar HTML documental; HTML embebido literal, nenhum `<script>` injectado), com checksum SHA-256 e manifesto (identificadores/versões, sem comparação de documentos); cópia via Clipboard API com tratamento de falha (só single_markdown; reavalia no servidor com operation=copy; confirmação visual; nada guardado no browser); descarga `.md`/`.zip` com nome de ficheiro do backend (Content-Disposition), criação e revogação da URL temporária, sem biblioteca externa (409 na descarga reavalia a análise); auxiliares mínimos no cliente HTTP central — `apiPostWithStatus` (não lança em 409/413; corpo transporta a análise) e `apiPostBlob` (binário + checksum header); execuções passam a área real na ficha (lista mantida; sem novo separador global); handoff manual simulado nos testes (gerar/confirmar/copiar/descarregar em manual_local e manual_external; sete secções/manifesto/ficheiros verificados; sem envio a serviço real; sem importar resposta; execuções permanecem prepared); determinismo/checksum garantidos no backend PR04 (a UI apresenta, não recalcula); confirmação com modelo externo real não declarada (fica para F1-P08/piloto); apenas frontend (nenhum ficheiro backend alterado; backend permanece 392 OK); não implementado importação de resultado/aprovação/aplicação/IA; 18 testes de componente (24 casos do guião); 91 frontend + build verdes; VAL-008/VAL-012/VAL-014 reforçadas (Parciais); **F1-P05 em execução 5/6** | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-14 08:00 | F1 / F1-P05 | PR06 | Concluído | Não revista | Validação integrada e fecho da F1-P05 (MVP-10/11/12): cenário E2E pela API — Product activo; documento `instrucoes` v1 `confirm`; função IA ligada (requires_approval=True); documentos A `allowed`/B `confirm`/C `denied`; execução `manual_local` com A v1+B v1; C `denied` não seleccionável (400); snapshots de função/produto e `instruction_version` congelados; editar função + criar v2 de instruções/A/B → execução continua ligada às v1 (conteúdo/checksums das v1; versão actual nunca substitui a seleccionada); gerar sem confirmar B+instruções → 409 `confirmation_required` sem conteúdo; confirmar → gera Markdown; nova geração com checksum idêntico; ZIP com manifesto+7 ficheiros de secção+documentos/; sete secções em ordem; fontes/versões/checksums presentes; conteúdo hostil permanece em DADOS entre marcadores; execução permanece `prepared`; política alterada depois — B→`denied` bloqueia com 409 sem conteúdo, B→`allowed` gera aplicando política actual, versões congeladas intactas; ciclo de vida da função (inactivar → nova execução rejeitada 400, execução passada mantém snapshot e gera pacote; reactivar) com isolamento entre 2 empresas (função/produto/versão alheios → 400; execução e preview alheios → 404; lista de B com count=0; tentativas cruzadas auditadas); concorrência real em PostgreSQL — 2 gerações simultâneas com checksums/bytes idênticos e execução inalterada; alteração de `export_policy` concorrente com a geração sempre coerente (documento completo ou bloqueado, nunca pacote parcial) com versões/snapshots imutáveis e estado final coerente; armazenamento — versões existem e checksum coincide, objecto ausente → 409 sem pacote parcial (storage.failure), ZIP sem path traversal, pacote não guardado na BD; segurança — documentos são DADOS com declaração anti-injecção, secções 1–6 inalteradas pelo conteúdo, Markdown não sanitizado/transformado/renderizado a HTML (UI em <pre> sem dangerouslySetInnerHTML), pacote sem cookies/tokens/segredos/env/metadados de auditoria, sem declarar protecção completa contra prompt injection; auditoria eventos 10/11/12 + cross-org, todos com correlation_id, sem objective/instruções/constraints/snapshots/pacote/conteúdo (provado com tokens); migrações — drift zero, aplicam em base vazia e na base de dev existente (functions.0001+executions.0001 aplicadas nesta validação, additivas; reverter remove tabelas destes módulos, outras apps intactas); evidência de geração registada (só checksum/estrutura, transacção revertida — MD checksum 4df4df5c… determinístico 1906 bytes 7 secções; ZIP checksum a98ec602… determinístico com manifest+secções+documentos/; exec prepared); simulação de handoff manual sem modelo real (copiar MD/descarregar ZIP nos testes; sem resposta fictícia, sem importar/aprovar/aplicar; execuções ficam prepared); regressão 396 backend (+4: integração+concorrência) + 91 frontend + build; health live/ready 200; Docker Compose healthy; nenhum defeito de aplicação — nenhuma correcção de código; nenhuma funcionalidade de F1-P06 antecipada; **VAL-007 e VAL-008 Validadas**, VAL-002/VAL-012/VAL-014 Parciais, VAL-009 não validável sem resultados; sem desvio estrutural → nenhuma decisão global; **F1-P05 Concluída 6/6**; próximo passo: commit de F1-P05 e gerar F1-P06 just-in-time | `03_fase_1_mvp/pipelines/05_funcoes_execucoes_pacote_contexto/resultados_execucao/prompt_06_resultado.md` |
| 2026-07-14 09:20 | F1 / F1-P06 | PR01 | Concluído | Não revista | Registo manual e imutável de resultados (MVP-13): modelo `ResultAttempt` (executions; UUID, organisation, execution PROTECT, attempt_number ≥1 único por execução, result_document_version→DocumentVersion PROTECT, imported_by PROTECT, source_mode pasted/file, source_tool obrigatório, source_model/source_notes opcionais, created_at) append-only — save/delete/queryset bloqueados; `AIExecution.current_result_attempt` FK nullable PROTECT gerada no servidor (aponta só para tentativa da própria execução; preserva-se após aprovação/rejeição/conclusão); migração `executions/0002` aditiva e segura para execuções prepared existentes, sem drift; serviço `import_result` atómico — bloqueia execução (select_for_update), valida expected_version+estado prepared, determina attempt_number no servidor, valida entrada (exactamente uma de content colado/file UTF-8 — ambos ou nenhum→400; limite DOCUMENT_MAX_BYTES→413; binário/UTF-8 inválido→400; nome do ficheiro não usado como storage_key; source_tool obrigatório; source_mode derivado), materializa Document `resultado` + DocumentVersion inicial via serviço documental público (conteúdo só no armazenamento privado, checksum coincidente, título determinístico "Resultado (tentativa N) — <execução>", associado ao Product da execução, export_policy default vigente sem excepção), cria ResultAttempt, define current_result_attempt, aplica transição prepared→result_pending_validation pela política central, incrementa version uma vez e audita; importar≠aprovar≠aplicar (sem aprovação/aplicação automática); coordenação BD↔storage — falha de escrita não cria nada, falha de BD depois da escrita remove o objecto órfão (storage.failure se falhar), execução nunca muda de estado em importação falhada, sem transacção distribuída inexistente; API `/api/v1/executions/{id}/result-attempts` POST (JSON content ou multipart file; CSRF+sessão; entrada estrita rejeita organisation/attempt_number/document/status/internos; devolve tentativa+metadados da versão exacta+estado da execução), GET lista (crescente por attempt_number, sem conteúdo, sem chaves de storage) e GET detalhe (metadados+conteúdo da versão EXACTA da tentativa, nunca current_version, +contexto mínimo da execução, sem pacote/segredos/auditoria); 2.ª importação enquanto result_pending_validation→409; versão obsoleta→409; duas importações concorrentes→exactamente uma tentativa e uma transição (version=2); restrições à API documental genérica (requisito do prompt, não correcção silenciosa): POST /documents rejeita document_type=resultado (400); PATCH e restore rejeitam (409) documentos ligados a ResultAttempt (via relação inversa result_attempts, sem importar executions — evita ciclo), leitura permitida; compatibilidade — documentos resultado anteriores não apagados nem convertidos, sem migração de dados; dois testes documentais adaptados (UTF-8/filtro) por o contrato ter mudado; auditoria evento 13 (`result.imported`, entity=result_attempt) só com operation/execution_id/attempt_number/source_mode/document_id/document_version_id/checksum abreviado/execution_version — sem resultado/source_notes/source_tool/prompt/pacote/conteúdo (provado com tokens); acesso cruzado→404 auditado security.cross_org_attempt; não implementado UI/revisão/aprovação/rejeição/correcção/aplicação/parsing/IA/importação automática/anexos binários; 28 testes backend novos (27 requisitos + concorrência) + migração actualizada (3 tabelas); 424 backend + documentos 54 + 91 frontend verdes; sem drift; só backend (frontend inalterado); VAL-009 Parcial (falta UI e validação integrada), VAL-010 não validada, VAL-002/VAL-012/VAL-014 Parciais; sem desvio estrutural → nenhuma decisão global; **F1-P06 em execução 1/6** | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_01_resultado.md` |
| 2026-07-14 10:20 | F1 / F1-P06 | PR02 | Concluído | Não revista | Interface de importação, consulta e histórico de resultados (MVP-13.T2/T3): componentes `ResultAttemptsPanel`/`ResultImportForm`/`ResultAttemptView` acrescentados ao `ExecutionDetail` (que passa a manter cópia local da execução e a recarregá-la via getExecution após importar; ContextPackagePanel/snapshots/contexto preservados); orquestração por estado — `prepared` mostra formulário de importação; `result_pending_validation` mostra tentativa actual + indicação de revisão pendente (sem a implementar); `approved`/`rejected`/`completed` mostram histórico em leitura; sem botões de revisão ou aplicação; formulário com escolha explícita e exclusiva Colar resultado (textarea)/Carregar ficheiro (accept .md/.markdown/.txt) — nunca content e file juntos —, source_tool obrigatório, source_model/source_notes opcionais, limite de tamanho informativo (servidor é autoridade), envia expected_version actual, confirmação obrigatória que explica importar regista tentativa/não aprova/não aplica/tentativa imutável, guarda contra submissão duplicada (busy+disabled, um só POST), ficheiro mostra só o nome local sem FileReader nem persistência de caminho; depois de importar actualiza o detalhe (→ Resultado por validar), abre a tentativa criada, nada apresentado como oficial, documentos/decisões/pendências não mudam na UI (importar só toca em result-attempts/executions/documents-preview — testado); histórico crescente por attempt_number com data/origem/modo/modelo/checksum/versão documental, tentativa actual identificada, qualquer tentativa abrível, nunca usa current_version para substituir a versão da tentativa, sem editar/eliminar/recuperar; apresentação do conteúdo não confiável em duas vistas — texto original em <pre> não executável e pré-visualização SEGURA que reutiliza DocumentPreview (envia ao endpoint documents/preview e insere só o HTML sanitizado do backend; nunca renderiza Markdown no cliente; hostil <script> literal e nenhum script injectado); erro 413 apresentado claramente; erro 409 recarrega a execução e mostra o estado actual com aviso sem repetir automaticamente nem perder conteúdo silenciosamente; cliente HTTP central reutilizado com um auxiliar mínimo apiPostFormWithStatus para multipart/form-data (sem definir Content-Type — boundary do browser; mantém sessão/CSRF/tratamento uniforme; sem 2.º cliente); não implementado revisão/aprovação/rejeição/correcção/aplicação/parsing/IA/importação automática; 14 testes de componente (23 casos do guião); mocks de execuções/pacote actualizados com lista de tentativas vazia; 105 frontend + build verdes; só frontend (backend inalterado); VAL-009 Parcial (falta validação integrada), VAL-014 reforçada (Parcial até F1-P07); sem desvio estrutural → nenhuma decisão global; **F1-P06 em execução 2/6** | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_02_resultado.md` |
| 2026-07-14 11:45 | F1 / F1-P06 | PR03 | Concluído | Não revista | Revisão humana dos resultados importados (MVP-14.C1/H1/H2): modelo `ResultReview` (executions; UUID, organisation/execution/result_attempt/reviewer PROTECT, `decision` fechada {approved,rejected,correction_requested} — nunca campo genérico de estado —, `observations` opcional na aprovação e obrigatória na rejeição/correcção, created_at) **append-only** (save/delete/queryset bloqueados por `ResultReviewImmutableError`); constraints `unique(result_attempt)` (uma revisão por tentativa, defesa final de concorrência) + decisão fechada + observações obrigatórias quando decision≠approved; migração `executions/0003` sem drift; `review_service` com três comandos explícitos (`approve_result`/`reject_result`/`request_correction`) numa transacção que autoriza (só Owner activo — `membership.role`), valida observações, bloqueia execução (select_for_update)+valida expected_version+estado result_pending_validation, resolve e bloqueia a tentativa indicada exigindo que seja `current_result_attempt` (tentativa histórica/substituída→409), verifica ausência de revisão prévia (constraint como defesa final, IntegrityError→AlreadyReviewed), cria a revisão (full_clean), transita pela **política central** e incrementa `version` uma vez, e audita; aprovar `result_pending_validation→approved` — **aprovar ≠ aplicar**: não cria DocumentVersion, não altera Product/Decision/WorkItem, não cria aplicação e nunca vai a completed (provado); rejeitar `→rejected` terminal; pedir correcção `→prepared` preservando current_result_attempt+tentativa+revisão e permitindo nova importação (nº seguinte pelo mecanismo de PR01, sem eliminar o resultado anterior nem reabrir rejected/completed); endpoints explícitos `POST /api/v1/executions/{id}/result-attempts/{n}/approve|reject|request-correction` (entrada estrita: só expected_version obrigatório + observations; rejeita decision/reviewer/status/internos) e `GET /api/v1/executions/{id}/reviews` (histórico mínimo, sem conteúdo do resultado); respostas com revisão criada+execução actualizada+tentativa; autorização não-Owner→403, versão obsoleta→409, tentativa não-actual→409, dupla revisão→409 (concorrência: duas revisões simultâneas sobre a mesma tentativa produzem exactamente uma, a 2.ª conflito), revisão alheia→404 auditada; auditoria eventos 14–16 (`result.approved`/`result.rejected`/`result.correction_requested`, entity=result_review) com operation/attempt_number/review_id/transition/execution_version — sem resultado nem observações integrais (provado com tokens); dupla revisão negada auditada `denied` (fora da transacção revertida); acesso cruzado→404 + security.cross_org_attempt; UI `ValidationPanel` no `ExecutionDetail` (só em result_pending_validation; nos restantes estados fica o `ResultAttemptsPanel`) com tentativa actual (origem/conteúdo original/pré-visualização segura via ResultAttemptView), histórico de tentativas (abrível) e de revisões (só leitura), três acções separadas Aprovar/Rejeitar/Pedir correcção, confirmação de aprovação com texto explícito (aprovar valida o resultado / não aplica alterações / aplicação será operação posterior), observações obrigatórias na rejeição e correcção, botões desactivados durante a submissão, 409 recarrega o estado sem repetir, depois de correcção volta ao formulário de importação, sem qualquer acção de aplicação (nota explícita); contratos e desfechos tipados em `api/executions.ts` (`listReviews`/`reviewResult`); não implementado aplicação/alteração automática de documentos/aplicação parcial/diffs/notificações/aprovação automática/excepções à revisão humana/IA; 24 testes backend novos (revisão + concorrência) + 7 frontend (25–30) + 3 testes de componente PR02 ajustados ao novo estado pending; regressão 123 executions backend + 112 frontend + build; makemigrations --check sem drift; VAL-010 Parcial (aplicação e validação integrada em PR04+/PR06), VAL-009 Parcial (reforçada), M1 não declarado; sem desvio estrutural → nenhuma decisão global; **F1-P06 em execução 3/6** | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_03_resultado.md` |
| 2026-07-14 15:30 | F1 / F1-P06 | PR04 | Concluído | Não revista | Primeira aplicação oficial de um resultado aprovado — nova versão documental (MVP-15.C1/H1): modelo `ResultApplication` (executions; UUID, organisation PROTECT, `execution` **OneToOne** PROTECT — uma aplicação por execução, defesa final —, `result_attempt`/`review` PROTECT, `application_type` fechado {document,decision,work_item,no_change} com só `document` implementado, `applied_by` PROTECT, `request_fingerprint` SHA-256, `change_summary`/`rationale` curtos, alvos/criados PROTECT null `target_document`/`base_document_version`/`created_document_version`/`target_decision`/`created_decision`/`target_work_item`, created_at) **append-only** (save/delete/queryset bloqueados por `ResultApplicationImmutableError`); **sem GenericForeignKey** e **sem FK de executions dentro de DocumentVersion** (evita dependência circular) — a ligação oficial execução↔versão é `created_document_version`; constraints unicidade por execução + tipo fechado + coerência de `document` (alvo/base/criada presentes e resumo não vazio); migração `executions/0004` aditiva sem drift; `application_service.apply_document` numa transacção que: autoriza (só Owner activo), valida a confirmação explícita do contrato (`apply-document`) + conteúdo explícito obrigatório + resumo obrigatório, valida/encode o conteúdo (UTF-8+limite; checksum canónico), bloqueia a execução, verifica idempotência (`request_fingerprint` sobre execução/tentativa/tipo/alvo/versões esperadas/checksum do conteúdo/resumo normalizado) **antes** do estado — repetição idêntica devolve a aplicação existente (200) sem nova versão, comando diferente → 409 —, exige `approved` + `expected_execution_version`, tentativa **actual** com `ResultReview approved` (senão 409 auditável denied), bloqueia e valida o **documento alvo** (tem de pertencer ao **Product da execução**; empresarial/outro Product → 422; `resultado` ou gerido por ResultAttempt → 422; alheio → 404 auditado; `expected_document_version` → 409), escreve o objecto no armazenamento e cria a nova `DocumentVersion` via building blocks do serviço documental (todas as versões anteriores preservadas), cria a `ResultApplication` (guarda `base_document_version` e `created_document_version`) e transita `approved→completed` incrementando `version` uma vez — só passa a completed **depois** de a versão e a aplicação existirem; o servidor **nunca** extrai nem aplica automaticamente o conteúdo do resultado (o conteúdo aplicado é o revisto e confirmado pelo utilizador); atomicidade BD↔storage — falha de storage deixa `approved` (503 + `storage.failure`, sem escrever nada de BD), falha de BD depois da escrita remove o objecto órfão e reverte tudo (sem `completed` sem aplicação, sem aplicação sem versão, sem aplicação parcial), conflito deixa alvo e execução intactos; endpoints `POST /api/v1/executions/{id}/apply/document` (entrada estrita: attempt_id **ou** attempt_number, target_document, expected_execution_version, expected_document_version, content, change_summary, confirmation; rejeita application_type/review/versão criada/internos; 201 criado / 200 idempotente) e `GET /api/v1/executions/{id}/application` (aplicação ou 404); auditoria evento 17 `change.applied` (entity=result_application) com application_type/attempt_number/review_id/target_document_id/base_version/created_version/checksum abreviado/transition/execution_version — sem conteúdo aplicado nem change_summary integral (provado com tokens); tentativa sem aprovação auditada `denied`; acesso cruzado → 404 + security.cross_org_attempt; UI `ApplicationPanel` no `ExecutionDetail` (só em `approved`; `ExecutionDetail` passa a orquestrar prepared→importação/pending→revisão/approved→aplicação/restantes→histórico) com tentativa aprovada + revisão + resultado original (referência, nunca aplicado automaticamente), lista de documentos elegíveis do Product (exclui `resultado`; o backend é autoridade), versão actual do alvo, editor de conteúdo + `change_summary`, botão "Usar resultado como ponto de partida" (nunca aplica directamente), confirmação final explícita (conteúdo revisto / nova versão oficial / execução concluída), aviso de que a aprovação anterior **não aplicou nada**, 409 recarrega documento+execução sem sobrescrever, mostra a versão criada e a ligação à execução, **sem decisões nem pendências** (F1-P06-PR05); contratos e desfechos tipados em `api/executions.ts` (`applyDocument`/`getApplication`, `DOCUMENT_APPLY_CONFIRMATION`); reutiliza `api/documents.ts` (`listDocuments`/`getDocument`) sem 2.º cliente; não implementado decisão/pendência/fecho sem alteração/múltiplos alvos/parsing/diff/rollback/aplicação parcial/IA; 25 testes backend novos (aplicação + concorrência) + 5 frontend (30–32 + extras); regressão 117 frontend + build; makemigrations --check sem drift; VAL-010 Parcial (restantes caminhos em PR05, validação integrada em PR06), VAL-004 com evidência de versão aplicada, M1 não declarado; sem desvio estrutural → nenhuma decisão global; **F1-P06 em execução 4/6** | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_04_resultado.md` |
| 2026-07-14 16:30 | F1 / F1-P06 | PR05 | Concluído | Não revista | Completa a aplicação controlada com os caminhos decisão/pendência/fecho (MVP-15.C2/H2): regra global **uma execução produz no máximo uma `ResultApplication`** (unicidade por execução + `request_fingerprint` idempotente; não contornável por endpoints distintos); `application_service` refactorizado — prefixo comum `_prepare` (autoriza só-Owner activo, bloqueia a execução, verifica idempotência **antes** do estado, exige `approved`+`expected_execution_version`, resolve a tentativa actual e exige `ResultReview approved`) partilhado pelos quatro caminhos, `_complete_execution` (transita `approved→completed`, versão +1) e `_audit_apply` (evento 17 comum); **decisão** (`POST /apply/decision`) — decisão alvo **activa** do Product da execução (empresarial/outro Product → 422; superseded → 422; alheia → 404 auditada), campos da nova decisão fornecidos explicitamente pelo utilizador (título/contexto/decisão obrigatórios; sem parsing/estruturação automática do resultado), usa o serviço **existente** `supersede_decision` (nova `active`, anterior `superseded`, cadeia `supersedes` preservada, `expected_decision_version`→409), `ResultApplication` guarda `target_decision`+`created_decision`; **pendência** (`POST /apply/work-item`) — WorkItem `open` do Product (final → 422; outro Product → 422; alheio → 404), usa o comando **existente** `complete_work_item` (`open→completed`; não altera título/notas/tipo/prazo; `expected_work_item_version`→409), guarda `target_work_item`; **fecho** (`POST /close-without-application`) — `no_change` com `rationale` obrigatório + confirmação explícita, cria `ResultApplication` sem alvo e **sem** criar/alterar documento/decisão/pendência; todos exigem aprovação e a confirmação do contrato (valores distintos `apply-decision`/`apply-work-item`/`close-without-application`), concluem `approved→completed` e emitem `change.applied` (evento 17) distinguindo `application_type` e registando target/entidade criada + `transition` — sem resultado/observações/decision_text/notes/rationale integral (provado com tokens); tentativa sem aprovação → `denied`; acesso cruzado → `security.cross_org_attempt`+404; **uma aplicação por execução** provada — segundo caminho (mesmo de tipo diferente) após aplicação → 409, aplicação documental→decisão e decisão→pendência rejeitadas, concorrência entre dois tipos deixa exactamente um vencedor (uma versão/mutação, uma aplicação); idempotência (repetição idêntica → aplicação existente 200; diferente → 409); atomicidade (bloqueia execução e alvo; mutação+aplicação+transição na mesma transacção; falha não conclui a execução nem deixa entidade parcialmente alterada); constraints de coerência por `application_type` (decision: target+created; work_item: target; no_change: rationale) na migração `executions/0005` (aditiva, sem drift); entrada estrita por endpoint (rejeita `application_type`/`review`/versão criada/internos); UI `ApplicationPanel` completo com **quatro opções** (Criar nova versão documental / Substituir decisão / Concluir pendência / Fechar sem alteração), apresenta apenas alvos elegíveis (decisões `active`, pendências `open`, documentos não-`resultado` do Product), formulários explícitos da nova decisão / confirmação de conclusão / rationale — nenhum preenchido por parsing do resultado (consultável como referência), **resumo exacto da mutação** antes de confirmar, aviso de que a aprovação não aplicou nada e de que só é possível uma aplicação, 409 recarrega os dados sem sobrescrever, apresenta a aplicação final com ligação ao alvo e **impede segunda aplicação**; contratos e desfechos tipados em `api/executions.ts` (`applyDecision`/`applyWorkItem`/`closeWithoutApplication` + constantes de confirmação), reutiliza `api/decisions.ts`/`api/workItems.ts` sem 2.º cliente; provas negativas (importar/aprovar não alteram Decision/WorkItem; só apply/close leva a completed; nenhum estado não-approved aplica); não implementado múltiplas aplicações/aplicação parcial/criação automática de pendências/decisão gerada automaticamente/parsing/diff/rollback/agentes/IA; 19 testes backend novos (18 caminhos + 1 concorrência entre tipos) + 7 frontend (28–32 + fecho/409); regressão 124 frontend + build; PR04 documento (25) reverificado verde após o refactor; makemigrations --check sem drift; VAL-010 Parcial (validação integrada E1–E6 e M1 em PR06), VAL-004/005/006 com evidência de aplicação; sem desvio estrutural → nenhuma decisão global; **F1-P06 em execução 5/6** | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_05_resultado.md` |
| 2026-07-14 17:30 | F1 / F1-P06 | PR06 | Concluído | Não revista | Validação de fecho de F1-P06 (MVP-13/14/15; marco M1): teste E2E dedicado `test_pipeline_e2e` percorre o **fluxo vertical completo** pelos endpoints reais — preparar execução → importar tentativa 1 (texto) → `result_pending_validation` → (nenhuma entidade oficial muda; sem `ResultApplication`) → pedir correcção com observações → `prepared` (tentativa 1 e review `correction_requested` preservadas) → importar tentativa 2 (ficheiro, `attempt_number=2`) → aprovar tentativa 2 → `approved` (documento alvo intacto) → aplicar a documento → nova `DocumentVersion` (base preservada, ligada por `created_document_version`/`base_document_version`) + `ResultApplication` + `completed` → histórico e auditoria (eventos 13/16/13/14/17) confirmados, conteúdo exacto da versão da tentativa (nunca `Document.current_version`); cenários adicionais A–E (aplicar a documento; substituir decisão com cadeia `active`/`superseded`; concluir pendência preservando campos; fechar `no_change`; rejeitar → terminal, aplicação subsequente → 409); **provas negativas importar≠aprovar≠aplicar** por contagens/checksums (após importar e após aprovar: Product/documentos/decisões/pendências inalterados, sem aplicação, estados só `result_pending_validation`/`approved`; após aplicar: só o alvo escolhido muda, exactamente uma nova versão do alvo, os alvos não seleccionados intactos); isolamento entre 2 empresas (tentativas/revisões alheias 404; aplicação e alvos alheios 404 auditados `security.cross_org_attempt`); autorização (não-Owner não revê nem aplica → 403; `reviewer`/`applied_by` derivam da sessão, campos internos rejeitados por entrada estrita); corrigidos **2 defeitos de expectativa de teste** (não de produto): `test_approval_creates_no_application` passou a verificar ausência de **instância** `ResultApplication` (o modelo existe legitimamente desde PR04) e a prova negativa E2E passou a contar as versões do documento **alvo** e não o total global de `DocumentVersion`; acrescentado a `config/settings.py` um hasher rápido (MD5) **apenas** quando a suite de testes corre (`'test' in sys.argv`), sem qualquer efeito em produção, reduzindo o tempo da suite de ~30 min para ~90 s; regressão **backend completa 502 testes OK** (todas as apps: auth, onboarding, portefólio, documentos/versões, decisões, pendências, funções, execuções/pacote, armazenamento, auditoria append-only, executions incl. E2E e concorrência), **frontend 124 OK** + `tsc` + `vite build`; `makemigrations --check` **sem drift**; **reversibilidade estrutural** verificada em base controlada (forward em base vazia 0001–0005; reverter `executions`→0001 remove tentativas/revisões/aplicações; forward de novo; migrações de outras apps preservadas); migrações **aplicadas na base existente** do contentor (0001–0005 `[X]`); **stack Docker reconstruída e saudável** (imagens backend+frontend rebuild; `health/live`=200, `health/ready`=200; ping=200; os quatro endpoints de aplicação + revisão roteados — 403 sem sessão; frontend serve o bundle «VentureOps AI»); demonstração do fluxo executada na **imagem reconstruída** (10 testes E2E dentro do contentor `ventureops-backend-1`, 4.8 s) — a demonstração **visual** no browser ficou bloqueada por indisponibilidade temporária da ferramenta de browser (classificador offline), pelo que o fluxo foi demonstrado pelos endpoints reais do stack ao vivo + os 124 testes de componente das UIs; nenhuma IA chamada, sem parsing/diffs/rollback; **VAL-009 e VAL-010 Validadas**, VAL-004/005/006 com evidência de aplicação/M1, VAL-002/012/014 mantêm-se Parciais (consolidação F1-P07), VAL-011 inalterada; **M1 — fluxo vertical ponta a ponta — ATINGIDO**; sem desvio estrutural → nenhuma decisão global; **F1-P06 Concluída 6/6**; próximo passo: commit de F1-P06 e gerar F1-P07 just-in-time | `03_fase_1_mvp/pipelines/06_resultados_revisao_aplicacao/resultados_execucao/prompt_06_resultado.md` |
