# Configuração do frontend — VentureOps AI (F1-P02-PR03)
# Base da API usada pelo cliente HTTP central. Por defeito `/api` (mesma origem,
# via proxy de desenvolvimento do Vite). Não colocar segredos aqui.
VITE_API_BASE_URL=/api
