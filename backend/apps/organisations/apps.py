from django.apps import AppConfig


class OrganisationsConfig(AppConfig):
    """Empresa/workspace, memberships, papéis e isolamento de tenant."""

    name = "apps.organisations"
    label = "organisations"
