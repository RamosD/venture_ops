from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    """Produtos, estado, revisões, responsáveis e indicadores de atenção."""

    name = "apps.portfolio"
    label = "portfolio"
