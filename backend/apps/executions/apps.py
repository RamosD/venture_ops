from django.apps import AppConfig


class ExecutionsConfig(AppConfig):
    """Pedidos, snapshots, contexto, resultados, validações e aplicação."""

    name = "apps.executions"
    label = "executions"
